# Agent Guidelines

## Version nomenclature

Project versions MUST follow the `X.Y.Z` format:

- `X`: release version.
- `Y`: new features, alpha releases, or beta releases.
- `Z`: bug fixes.

Example: `2.3.1`.
