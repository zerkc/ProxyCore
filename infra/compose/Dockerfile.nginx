ARG NGINX_IMAGE=nginx:1.30.4-alpine-slim
FROM ${NGINX_IMAGE}

COPY infra/nginx/nginx.conf /etc/nginx/nginx.conf
COPY scripts/nginx-entrypoint.sh /usr/local/bin/nginx-entrypoint.sh

ENTRYPOINT ["/usr/local/bin/nginx-entrypoint.sh"]
